import 'package:flutter/material.dart';

class AdminCalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Admin Calendar Screen Content'),
      ),
    );
  }
}
